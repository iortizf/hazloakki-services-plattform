server.port: 8085
spring:
  application:
    name: account-ms
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update 
  datasource:
    platform: postgres
    url: jdbc:postgresql://account-db-service/hazloakki_cuenta
    username: admin
    password: admin
    driverClassName: org.postgresql.Driver